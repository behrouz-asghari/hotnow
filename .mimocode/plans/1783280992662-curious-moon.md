# HotNow: Dead Code Cleanup + Dark Premium UI Redesign

## Overview

Two-track plan: (1) clean dead code and unused files, (2) redesign all UI with a dark premium theme and rich animations.

---

## Track 1: Dead Code Cleanup

### Step 1: Delete dead files

| File | Reason |
|------|--------|
| `app/lib/sources/webScrapers.ts` | 139 lines, zero imports. Superseded by `ninisite.ts`, `karzar.ts`, `tgstat.ts`. |
| `app/components/TrendsTable.tsx` | Zero imports. Not rendered anywhere. |
| `app/assets/fonts.css` | Duplicate of `globals.css` font declarations. |

### Step 2: Extract duplicated `parsePersianInt`

Identical 8-line function in `ninisite.ts:4-11` and `tgstat.ts:4-11`.

- Add `parsePersianInt` to `app/lib/utils/text.ts`
- Update `ninisite.ts` and `tgstat.ts` to import from `../utils/text`

### Step 3: Clean config

Remove 3 unused values from `app/lib/config.ts`:
- `openRouterTimeoutMs` — never referenced
- `maxItemsPerSource` — never referenced
- `aiCacheTtlSec` — never referenced

Keep `sourceTimeouts` with a "reserved" comment.

### Step 4: Delete dead prompt function

Delete `buildPersianAnalysisPrompt` from `app/lib/ai/prompts.ts` (lines 22-40) — never imported, superseded by `buildGeneralAnalysisPrompt`.

### Step 5: Remove unused import

`app/components/AnalysisProgress.tsx:3` — remove `SOURCE_NAMES` from import (unused).

### Step 6: Type remaining `any` annotations

| File | Line | Fix |
|------|------|-----|
| `app/lib/sources/wikiTop.ts` | 21-22 | Define `WikiArticle` interface, replace `(a: any)` |
| `app/lib/sources/googleTrends.ts` | 17 | Define `GoogleTrendItem` interface, replace `(it: any)` |
| `app/lib/sources/fetchWithTimeout.ts` | 13, 28 | Define `TimeoutError` interface, use type narrowing |
| `app/lib/sources/digikala.ts` | 3 | Define `DigikalaProduct` interface, replace `AnyRecord` |
| `app/page.tsx` | 34 | Replace `(data.items as any)` with `(data.items as ClusteredItem[])` |

### Step 7: Delete stale plan files

Delete all files in `.mimocode/plans/` — the refactoring they describe is complete.

---

## Track 2: Dark Premium UI Redesign

### Step 8: Install dependencies

```bash
npm install framer-motion lucide-react
```

- `framer-motion` — page transitions, staggered lists, hover effects
- `lucide-react` — tree-shakeable icon library, replaces all emojis

### Step 9: Rewrite `app/globals.css`

Complete rewrite with dark theme design system:
- CSS variables: `--bg-primary: #0f172a`, `--bg-secondary: #1e293b`, `--text-primary: #f1f5f9`, etc.
- Neon accent variables: blue `#3b82f6`, violet `#8b5cf6`, cyan `#06b6d4`
- Glow variables: `--glow-blue: 0 0 20px rgba(59,130,246,0.3)`
- Utility classes: `.glass` (glass morphism), `.gradient-border`, `.glow-card`
- Dark scrollbar styling
- Keep Vazirmatn font-face declarations

### Step 10: Update `app/layout.tsx`

Change body from `bg-gray-50 text-gray-900` to `bg-[#0f172a] text-[#f1f5f9] min-h-screen antialiased`.

### Step 11: Redesign `app/page.tsx`

- Replace `🔥` emoji with Lucide `TrendingUp` icon
- Gradient text title: `bg-gradient-to-l from-blue-400 via-violet-400 to-cyan-400 bg-clip-text text-transparent`
- Wrap content in `motion.div` with entrance animation

### Step 12: Redesign `StatCards.tsx`

Replace raw percentages with animated SVG circular progress rings:
- Lucide icons: `Brain`, `Zap`, `AlertTriangle`, `Compass`, `TrendingUp`, `Magnet`
- SVG gauge with `motion.circle` stroke animation
- Dark cards: `bg-[#1e293b] border border-[#334155]` with hover glow
- Responsive grid: `grid-cols-2 sm:grid-cols-3 lg:grid-cols-6`
- Staggered entrance animation via Framer Motion

### Step 13: Redesign `AnalysisPanel.tsx`

- Glass morphism container: `bg-[#1e293b]/70 backdrop-blur-sm`
- Lucide icons: `Brain`, `BarChart3`, `Users`, `ShoppingCart`
- Hover glow on non-empty reports
- Framer Motion fade-in

### Step 14: Redesign `TrendsLabelsBoard.tsx`

- Dark chip colors: `bg-blue-500/10 text-blue-300 border-blue-500/20` (per source)
- Container: `bg-[#1e293b]/70 backdrop-blur-sm`
- Staggered section animations

### Step 15: Redesign `AnalysisProgress.tsx`

- Gradient progress bar: `bg-gradient-to-r from-blue-500 via-violet-500 to-cyan-500`
- Replace emoji stage indicators with Lucide: `CheckCircle2`, `Loader2`, `Circle`
- Dark stage chips with color-coded states

### Step 16: Redesign `SourceStatusGrid.tsx`

- Replace emojis with Lucide: `CheckCircle2`, `AlertTriangle`, `XCircle`, `Clock`
- Dark grid items: `bg-[#0f172a] border-[#334155]` with hover transition

### Step 17: Redesign `RefreshButton.tsx`

- Gradient button: `bg-gradient-to-r from-blue-600 to-violet-600`
- Hover glow: `shadow-blue-500/20 hover:shadow-blue-500/40`
- Lucide `Loader2` spinner

### Step 18: Redesign `loading.tsx` and `error.tsx`

- Loading: animated dual-ring gradient spinner
- Error: dark card with `AlertTriangle` icon, gradient retry button

### Step 19: Redesign `AnalysisLoader.tsx`

- Error: `bg-rose-500/10 border-rose-500/20 text-rose-300`
- Loading: dark card with gradient spinner
- Success: `bg-emerald-500/10 border-emerald-500/20 text-emerald-300`
- Warning: `bg-amber-500/10 border-amber-500/20 text-amber-300`

---

## Verification

1. `npm run build` — zero errors
2. All pages render in dark theme
3. Circular gauge animations work on StatCards
4. Hover glow effects on all cards
5. Staggered entrance animations on page load
6. All Lucide icons render correctly
7. RTL layout preserved throughout
8. Vazirmatn font renders at all weights
9. No data regression (reports, trends, sentiment values)
10. Responsive: mobile 1-col → tablet 2-3 col → desktop 6-col
